{
  "name": "danangauction-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.10.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.27.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "backend": "cmd /c \"cd .. && mvn spring-boot:run\"",
    "frontend": "REACT_APP_API_BASE=http://localhost:8080/api react-scripts start",
    "dev": "concurrently \"npm run backend\" \"npm start\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "resolutions": {
    "svgo": "^3.0.2",
    "@svgr/webpack": "^8.1.0",
    "postcss": "^8.4.31"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
